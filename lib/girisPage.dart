import 'package:flutter/material.dart';

class GirisPage extends StatelessWidget {
  const GirisPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('benim sayfam '),
      ),
    );
  }
}
