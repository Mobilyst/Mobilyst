import 'package:flutter/material.dart';

class HaritaPage extends StatelessWidget {
  const HaritaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('gorev verilemdi sayfasi'),
      ),
    );
  }
}
